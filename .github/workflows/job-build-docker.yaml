name: "Build"

on:
  workflow_call:
    inputs:
      OVERLAY:
        required: true
        type: string

    outputs:
      OVERLAY:
        description: "OVERLAY"
        value: ${{jobs.build-docker.outputs.OVERLAY}}

jobs:
  build-docker:
    runs-on: ubuntu-latest
    outputs:
      OVERLAY: ${{steps.out.outputs.OVERLAY}}

    steps:
      - name: Checkout Git repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Print
        run: echo "${{inputs.OVERLAY}}"

      - name: Output
        id: out
        run: echo "::set-output name=OVERLAY::${{inputs.OVERLAY}}"
